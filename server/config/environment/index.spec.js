'use strict';

var environment = require('./index.js');


/*
describe('Required Process Env:', function () {

    it('should have valid env', function () {
        console.log(environment.requiredProcessEnv("test"));
        expect(environment.requiredProcessEnv("test")).to.have.property('symbols');
    });
    
});

*/
